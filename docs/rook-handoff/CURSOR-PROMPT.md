Continue the current InZone PR #35 branch and preserve all work in progress. This package is Jayme's approved companion visual/motion direction.

First finish the already-authorized functional work: uninterrupted voice during gameplay, latency, grounded game context and working invitations. Do not restart those investigations or change providers just to adopt this design.

Then replace the temporary companion presentation using START-HERE.md, concept-board.png and motion-preview.html. Use ribbon-renderer.js as a motion reference, not an unreviewed production drop-in. The final expressive motion is approved. Earlier slower versions are superseded.

Keep the existing audio/session/quota logic. Wire the visual to actual idle/listening/thinking/playback/muted/error states; drive speaking deformation from smoothed real output audio when available. Preserve cancellation, background behavior, reduced motion, game focus, and iframe/document identity. No perpetual decorative speech loop. Keep voice enable, mic mute, interrupt/end and optional captions usable with clear meanings.

Implement in the existing player without redesigning the hub yet. Never use the illustrative game scene in concept-board.png as product content. No giant chatbot panel, fake waveform, generic glowing orb, or game-remounting layout transitions.

Return one immutable Preview, exact tested SHA, a short real-flow recording, and any remaining acceptance gap. Keep PR draft; no merge or production deployment. Do not alter ads, auth, Firestore rules or billing.

The whole-product visual redesign comes next, beginning with arrival/discovery. Do not preempt that separate visual review by reworking the catalogue now.

